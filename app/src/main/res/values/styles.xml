<resources>

    <style name="Theme.BargainBytes" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Set AppCompat’s actionBarStyle -->

        <!-- Set AppCompat’s color theming attrs -->
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorPrimaryDark">@color/primary_color_dark</item>
        <item name="colorAccent">@color/accent_color</item>



        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>

        <item name="android:textColorPrimary">@color/primary_text_color</item>
        <item name="android:textColorPrimaryInverse">@color/primary_text_color_inverse</item>
        <item name="android:textColorSecondary">@color/secondary_text_color</item>

        <item name="android:toolbarStyle">@style/Theme.ToolbarStyle</item>
    </style>

    <style name="Theme.ToolbarStyle" parent="ThemeOverlay.AppCompat.Dark.ActionBar">

    </style>

    <style name="PrimaryButton" parent="Widget.AppCompat.Button.Colored">
        <item name="colorButtonNormal">@color/primary_color</item>
        <item name="colorControlHighlight">@color/accent_color</item>
    </style>

    <style name="DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@android:color/white</item>
    </style>

    <style name="customTabLayout" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">1sp</item>
    </style>

    <style name="Toolbar.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">18sp</item>
    </style>

    <style name="MyTabLayoutTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">12sp</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>
